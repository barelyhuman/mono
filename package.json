{
  "name": "mono",
  "version": "0.0.1",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "web:dev": "yarn workspace @mono/web start",
    "core:dev": "yarn workspace @mono/core dev",
    "web:build": "yarn workspace @mono/web build",
    "core:start": "yarn workspace @mono/core start",
    "components:build": "yarn workspace @mono/components build",
    "shared:build": "yarn workspace @mono/shared build",
    "build": "yarn workspaces run build"
  }
}
